1. loss/target/my_scatter.csv是一堆散点，在执行任务前，先读取这个文件中的散点数据。
2. 优化param文件夹下的func.md中的两个式子中的常数项，使得优化后的函数能够尽可能拟合第2条中读取到的散点。
3. param文件夹下func.md中的两个式子彼此独立，计算第二个式子的时候，不允许参考第一个式子的结果！！！
4. 两个式子中的系数项最多优化5次，即最多与散点数据比较5次。
5. 不允许用任何自动化脚本或现成算法！！！！！自己手动去调整系数！
6. 输出每次优化的过程，即你为什么要这么调整某个系数的思考过程。
7. 最后输出每个式子优化的结果，并给出两个式子的最终误差。